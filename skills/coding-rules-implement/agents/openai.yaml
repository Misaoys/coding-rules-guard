interface:
  display_name: "编码规则：执行"
  short_description: "按 Plan 和机器校验范围执行修改"
  default_prompt: "使用 $coding-rules-implement 按当前会话主模型 Plan 原样执行；先以 session_main record-plan 并通过 v4 session-main/executor 门禁，再最小修改并检查 WRITE 越界。"
