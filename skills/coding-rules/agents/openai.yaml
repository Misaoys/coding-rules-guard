interface:
  display_name: "编码规则路由"
  short_description: "只选择 FAST/FULL 和当前阶段"
  default_prompt: "使用 $coding-rules 选择 FAST/FULL 和当前阶段；只做路由，按当前会话主模型 Plan → Luna WRITE → Verify → 当前会话主模型审核传递，不改写 Plan。"
