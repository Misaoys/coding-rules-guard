{
  "schema_version": 4,
  "delegation": {
    "planner_profile": "session_main",
    "policy": "all_write_tasks",
    "executor_profile": "executor_default",
    "reviewer_profile": "session_main",
    "review_required_for": "all_write_tasks",
    "read_only_policy": "no_delegation"
  },
  "profiles": {
    "executor_default": {
      "model": "gpt-5.6-luna",
      "reasoning_effort": "max"
    },
    "session_main": {
      "source": "session_main"
    }
  },
  "fallback": {
    "when": "a protected planner or executor profile is unavailable, or the current session main model cannot be recorded for review",
    "strategy": "block the gate and require an explicit configuration repair or main-model record; never substitute a different role",
    "must_report": true
  }
}
